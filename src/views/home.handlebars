<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <h1>{{title}}</h1>

  {{#if products}}
  <ul>
    {{#each products}}
    <li>
      <strong>{{this.name}}</strong> - R$ {{this.price}}
      <p>{{this.description}}</p>
    </li>
    {{/each}}
  </ul>
  {{else}}
  <p>{{message}}</p>
  {{/if}}

  <a href="/addproduct"><button>Adicionar Produto</button></a>
</body>

</html>